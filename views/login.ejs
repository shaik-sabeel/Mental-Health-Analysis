<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/style_login.css">
</head>
<body>
    <div class="container">
        <div class="login-box">
            <h2>Login</h2>
            <form id="loginForm" action="/login_submit" method="post">
                <div class="input-group">
                    <input type="email" id="email" name="email" required>
                    <label for="email">Email</label>
                </div>
                <div class="input-group">
                    <input type="password" id="password" name="password" required>
                    <label for="password">Password</label>
                </div>
                <div class="captcha-container">
                    <div class="captcha-box">
                        <span id="captchaText"><%= captcha %></span>
                    </div>
                    <input type="text" id="captchaInput" name="captcha" placeholder="Enter CAPTCHA" required>
                    <a href="/login"><button type="button" id="refreshCaptcha">↻</button></a>
                </div>
                <button type="submit" class="login-btn">Login</button>
            </form>
        </div>
    </div>
    <!-- <script src="/script_login.js"></script> -->
    <% if(locals.msg){ %>
        <!-- <script src="/alert.js"></script> -->
        <script>alert("Email or password incorrect, try again");</script>
        <% } %>
        <% if(locals.captchaMsg){ %>
            <!-- <script src="/alert.js"></script> -->
            <script>alert("Captcha incorrect, try again");</script>
            <% } %>
</body>
</html>