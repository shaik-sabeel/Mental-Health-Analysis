<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mental Health Support</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #eaecc6;
            text-align: center;
            color: #2BC0E4;
        }
        h1 {
            font-size: 3em;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 10px;
        }
        p {
            font-size: 1.4em;
            margin-bottom: 30px;
            font-weight: 300;
        }
        .button-container {
            display: flex;
            gap: 20px;
        }
        button {
            padding: 15px 30px;
            font-size: 1.2em;
            font-weight: bold;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        .register {
            background: #2BC0E4;
            color: white;
        }
        .login {
            background: #2BC0E4;
            color: #eaecc6;
        }
        button:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<body>
    <h1>Welcome to Mental Health Support</h1>
    <p>Your safe space for guidance and support.</p>
    <div class="button-container">
        <button class="register" url="/register" onclick="location.href='/register'">Register</button>
        <button class="login" url="/login" onclick="location.href='/login'">Login</button>
    </div>
</body>
</html>